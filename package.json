{
  "name": "tracktrek",
  "version": "0.1.0",
  "private": true,
  "description": "ASTA fixed-prize competitions and leagues. Backend serves the web pilot; mobile/ also builds native.",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "install:deps": "npm ci --prefix mobile && npm ci --prefix backend",
    "build": "bash scripts/build-web.sh",
    "db:migrate:deploy": "npm --prefix backend exec -- prisma migrate deploy --schema backend/prisma/schema.prisma",
    "start": "node backend/start.cjs"
  }
}
