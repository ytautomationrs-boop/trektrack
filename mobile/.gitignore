node_modules/
.expo/
dist/
*.log

# Secrets — real backend URL / keys live here for local dev.
.env
.env.*
!.env.example

# Generated by `expo prebuild` — regenerate with `npx expo prebuild`,
# don't hand-edit or commit (config lives in app.json).
/android/
/ios/
